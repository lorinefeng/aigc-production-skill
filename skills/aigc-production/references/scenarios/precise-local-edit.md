# 精确局部修复

先确定允许变化的最小区域和必须保护的相邻区域。局部编辑 prompt 必须同时写“改什么”和“守住什么”。

## 操作顺序

1. 从最近一张已知正确的底图开始。
2. 用 mask、归一化坐标框或清晰视觉标记圈定区域。
3. 说明编辑后必须移除标记，并自然融合边缘、纹理和光影。
4. 对变化区做细节 QA，对保护区做像素差或并排比对。
5. 保护区发生明显漂移时直接失败，不接受“整体更好”的替代解释。

文字和 Logo 修复优先使用可控后期合成。生成模型无法可靠复现时，不继续堆叠 prompt。
